# Adding Titles with React 19

Now that we have the styling down, let's add some titles to our pages!

For any sort of public, user-facing website it is of utmost importance to have proper titles
and SEO metadata for your pages. This helps with search engine rankings, accessibility,
and overall user experience.

Ever since React 19 came out it has become trivial to add titles and other metadata
to your pages using their hoisting mechanism that automatically collects metadata from
all your JSX and hoists it into the documents head.

This JSX:

```jsx
function SomeComponent() {
	return (
		<>
			<h1>Hello World</h1>
			<title>My Page Title</title>
			<meta name="description" content="My Page Description" />
		</>
	)
}
```

Would hoist the `<title>` and `<meta>` tags into the document head for you automatically,
which would produce the following HTML:

```html
<head>
	<title>My Page Title</title>
	<meta name="description" content="My Page Description" />
</head>
<body>
	<div id="root">
		<h1>Hello World</h1>
	</div>
</body>
```

Now that you understand how the hoisting mechanism works, let's see how we can use
this to our advantage by adding titles to our pages using React.

Every route in the project has instructions on what you need to do, go Koala hunting
and try to add titles to all the pages!

📜 https://react.dev/blog/2024/12/05/react-19#support-for-metadata-tags

## Exercise overview

In this exercise, we're going to add titles to our pages using React 19 JSX metadata tags.

1. Go through every route, Kody will be there to guide you in what you need to do.
2. Add titles to all the pages using React 19 JSX metadata tags.
3. Make sure to test your titles by visiting the pages and checking the document title in the
   browser tab and also in the document head using the browser dev tools.
